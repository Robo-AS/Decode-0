package org.firstinspires.ftc.teamcode.programs.CustomPathingLibrary;

public interface HeadingProfile {
    /** Return desired heading (radians) at arc-length s in [0, totalLength]. */
    double headingAt(double s);
}
